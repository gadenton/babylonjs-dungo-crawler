# Repository Guidelines

## Package Manager
- Always use `pnpm` (e.g., `pnpm install`, `pnpm dev`, `pnpm run <script>`, `pnpm add`) instead of `npm` or `yarn` for all package management, dependency installation, and script execution in this repository.
